# Provenance records

`vector_tongue_priority_manifest.json` records supplied hashes separately from
the code and scientific claims.

The referenced artifacts are intentionally not represented as verified unless
their bytes are available and produce the recorded hashes. Polygon verification
also remains pending until a transaction hash, contract, token ID, wallet, and
content URI can be checked.
